## What this changes

## How you tested it

- [ ] `npm run lint`, `npm run check`, and `npm test` pass (`npm run format` fixes most lint failures)
- [ ] If you touched matching or audio: `npm run e2e` passes against a running `./followspot`
- [ ] If you touched the page: tried it in a browser with a real mic
