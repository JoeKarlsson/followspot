## What this changes

## How you tested it

- [ ] `npm run check` and `npm test` pass
- [ ] If you touched matching or audio: `npm run e2e` passes against a running `./prompter`
- [ ] If you touched the page: tried it in a browser with a real mic
